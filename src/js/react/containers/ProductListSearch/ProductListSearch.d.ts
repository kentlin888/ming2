import * as React from 'react'
export interface ProductListSearchProps {
    //data ?:[any];
    //Requireable<any[]>
    //children: JSX.Element;
    //cc:number;
}
export interface ProductListSearchStates {
    // groupedProducts: number;
    // cc: number;
    //data ?:[any];
    //Requireable<any[]>
    //children: JSX.Element;
}
declare class ProductListSearch extends React.PureComponent < ProductListSearchProps, ProductListSearchStates > {
    // state:Readonly<ProductListSearchStates>;
}
export default ProductListSearch;